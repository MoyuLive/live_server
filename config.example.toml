# 日志配置
[log]
level = "info"
format = "json"
path = "logs/live_server.log"
console = true
console_level = "info"

# 数据库配置
[database]
url = "sqlite://live.db"

# SRS服务器配置
[[srs]]
api_url = "http://localhost:8080/api/v1"
